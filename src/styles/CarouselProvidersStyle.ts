import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
	image: {
		aspectRatio: 1 / 1,
		width: 40
	},
	imageContainer: {
		margin: 5
	},
	container: {
		height: 50
	}
});

export default styles;
