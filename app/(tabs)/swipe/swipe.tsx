import { Text, View } from "react-native";

export default function Swipe() {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <Text>Swipe</Text>
    </View>
  );
}
